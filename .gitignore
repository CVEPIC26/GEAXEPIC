# Dependencies
node_modules/

# Build output
dist/
build/

# Environment / secrets
.env
.env.local
.env.*.local
*.local

# Google Apps Script credentials (never commit)
# NOTE: apps-script/appsscript.json is a MANIFEST (scopes/webapp config) and
# MUST be committed — it is not a secret. Only credential files are ignored.
.clasp.json
.clasprc.json
*credentials*.json
*service-account*.json
*secret*.json
*token*.json
*oauth*.json

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Temporary / cache
.agent_tmp/
.cache/
tmp/
